<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }
    </style>
    <title>Home page</title>
</head>

<body>
    <h1> This is the URL shortener web page</h1>
    <% if(locals.id) { %>
        <p> URL generated is : https://localhost:8080/url/<%= id %>
        </p>
        <% } %>
            <div>
                <form method="POST" action="/url">
                    <label>Enter your original URL : </label>
                    <input type="text" name="url" placeholder="https://example.com" />
                    <button type="submit">Generate the Link</button>
                </form>
            </div>
            <div>
                <% if (locals.urls) { %>
                    <table>
                        <thead>
                            <tr>
                                <th>S.No</th>
                                <th>ShortId</th>
                                <th>Redirect Id</th>
                                <th>Clicks</th>
                            </tr>

                        </thead>
                        <tbody>
                            <% urls.forEach((url,index)=> { %>
                                <tr>
                                    <td>
                                        <%= index+1 %>
                                    </td>
                                    <td>
                                        <%= url.shortId %>
                                    </td>
                                    <td>
                                        <%= url.redirectUrl %>
                                    </td>
                                    <td>
                                        <%= url.visitHistory.length %>
                                    </td>
                                </tr>
                                <% }) %>


                        </tbody>
                    </table>
                    <% } %>
            </div>

</body>

</html>